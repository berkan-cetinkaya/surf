when:
  event: [ push, pull_request, tag, deployment, manual ]

steps:
  install:
    image: bash
    commands:
      - npm install
      - npm install --prefix examples/server

  build:
    image: bash
    commands:
      - npm run build

  test:
    image: bash
    commands:
      - npm test
