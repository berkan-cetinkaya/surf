<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nested Components - SURF Example</title>
    <style>
        :root {
            --glass-bg: rgba(30, 41, 59, 0.4);
            --glass-border: rgba(255, 255, 255, 0.08);
            --accent-primary: #38bdf8;
            --accent-secondary: #818cf8;
            --text-main: #f8fafc;
            --text-dim: #94a3b8;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: radial-gradient(circle at top right, #1e1b4b, #0f172a);
            background-attachment: fixed;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 4rem 2rem;
            color: var(--text-main);
        }

        .container {
            width: 100%;
            max-width: 800px;
        }

        h1 {
            font-size: clamp(1.5rem, 4vw, 2.5rem);
            font-weight: 800;
            margin-bottom: 1rem;
            background: linear-gradient(135deg, #fff 0%, var(--accent-primary) 100%);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: -0.04em;
            text-align: center;
        }

        p.description {
            color: var(--text-dim);
            margin-bottom: 3rem;
            text-align: center;
            line-height: 1.6;
            font-size: 1.1rem;
        }

        .back {
            position: fixed;
            top: 2rem;
            left: 2rem;
            color: var(--text-dim);
            text-decoration: none;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            transition: all 0.2s;
            z-index: 10;
        }

        .back:hover {
            color: var(--accent-primary);
            transform: translateX(-5px);
        }

        /* Card Styles */
        .card {
            background: var(--glass-bg);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border: 1px solid var(--glass-border);
            padding: 2.5rem;
            margin-bottom: 2rem;
            border-radius: 32px;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
            width: 100%;
            position: relative;
        }

        .outer-card {
            border-top: 4px solid var(--accent-secondary);
        }

        .inner-card {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid var(--glass-border);
            border-top: 4px solid var(--accent-primary);
            margin-left: 2rem;
            margin-top: 2rem;
            padding: 2rem;
            border-radius: 24px;
        }

        h2,
        h3 {
            margin-bottom: 1.5rem;
            font-weight: 800;
            letter-spacing: -0.02em;
        }

        h2 {
            color: var(--accent-secondary);
            font-size: 1.5rem;
        }

        h3 {
            color: var(--accent-primary);
            font-size: 1.25rem;
        }

        .btn {
            background: rgba(255, 255, 255, 0.05);
            color: var(--text-main);
            border: 1px solid var(--glass-border);
            padding: 0.6rem 1.2rem;
            border-radius: 12px;
            cursor: pointer;
            margin-right: 0.75rem;
            margin-bottom: 0.75rem;
            transition: all 0.2s cubic-bezier(0.23, 1, 0.32, 1);
            font-size: 0.9rem;
            font-weight: 600;
        }

        .btn:hover {
            background: rgba(255, 255, 255, 0.1);
            border-color: rgba(255, 255, 255, 0.2);
            transform: translateY(-2px);
        }

        .btn:active {
            transform: translateY(0);
        }

        .status-badge {
            display: inline-block;
            padding: 0.35rem 1rem;
            border-radius: 100px;
            font-size: 0.8rem;
            font-weight: 800;
            margin-left: 0.5rem;
            background: rgba(56, 189, 248, 0.1);
            border: 1px solid rgba(56, 189, 248, 0.2);
            color: var(--accent-primary);
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        .debug-text {
            color: var(--text-dim);
            font-size: 0.85rem;
            margin-top: 1.5rem;
            display: block;
            border-top: 1px solid var(--glass-border);
            padding-top: 1rem;
            font-style: italic;
        }

        .highlight {
            color: var(--text-main);
            font-weight: 700;
        }

        @media (max-width: 768px) {
            .inner-card {
                margin-left: 0;
            }

            body {
                padding: 6rem 1rem 2rem;
            }
        }
    </style>
</head>

<body>
    <a href="/showcase/01-interaction" class="back" d-pulse="navigate">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"
            stroke-linecap="round" stroke-linejoin="round">
            <line x1="19" y1="12" x2="5" y2="12"></line>
            <polyline points="12 19 5 12 12 5"></polyline>
        </svg>
        Back
    </a>

    <div class="container">
        <h1>Nested Components</h1>
        <p class="description">
            Multiple <code style="color: var(--accent-primary)">d-cell</code> components maintain isolated states, even
            when sharing identical variable names.
        </p>

        <!-- 
          OUTER COMPONENT: Project Manager 
          State: { mode: 'view' }
        -->
        <div class="card outer-card" d-cell="{ mode: 'view' }" d-id="outer-card">
            <h2>📦 Project Manager (Outer Scope)</h2>
            <p style="margin-bottom: 1.5rem;">
                Root Access: <span class="status-badge" d-text="mode"></span>
            </p>

            <div style="margin-bottom: 2rem;">
                <button class="btn" d-signal="click: mode = 'view'">View Context</button>
                <button class="btn" d-signal="click: mode = 'admin'">Admin Context</button>
            </div>

            <!-- 
              INNER COMPONENT: Task Item 1
            -->
            <div class="card inner-card" d-cell="{ mode: 'pending', title: 'Refactor Core Logic' }" d-id="inner-card-1">
                <h3>📄 Task Item 1 (Inner Scope)</h3>

                <p style="margin-bottom: 1rem">Task: <span class="highlight" d-text="title"></span></p>

                <p>
                    Local Status: <span class="status-badge" d-text="mode"></span>
                </p>

                <div style="margin-top: 1.5rem;">
                    <button class="btn" d-signal="click: mode = 'pending'">Pending</button>
                    <button class="btn" d-signal="click: mode = 'progress'">Active</button>
                    <button class="btn" d-signal="click: mode = 'done'">Resolved</button>
                </div>

                <small class="debug-text">
                    Isolated variable 'mode' is <span class="highlight" d-text="mode"></span>. Changes here don't leak
                    to parent.
                </small>
            </div>

            <!-- 
              INNER COMPONENT: Task Item 2
            -->
            <div class="card inner-card" d-cell="{ mode: 'done', title: 'Update Brand Guidelines' }"
                d-id="inner-card-2">
                <h3>📄 Task Item 2 (Inner Scope)</h3>
                <p style="margin-bottom: 1rem">Task: <span class="highlight" d-text="title"></span></p>
                <p>
                    Local Status: <span class="status-badge" d-text="mode"></span>
                </p>
                <div style="margin-top: 1.5rem;">
                    <button class="btn" d-signal="click: mode = 'pending'">Pending</button>
                    <button class="btn" d-signal="click: mode = 'progress'">Active</button>
                    <button class="btn" d-signal="click: mode = 'done'">Resolved</button>
                </div>
            </div>

            <div style="margin-top: 2rem; border-top: 1px solid var(--glass-border); padding-top: 1.5rem;">
                <p style="color: var(--text-dim); font-size: 0.95rem;">
                    Parent variable 'mode' remains: <span class="highlight" d-text="mode"></span>
                </p>
            </div>
        </div>
    </div>

    <%- include('../../partials/core-scripts') %>
</body>

</html>