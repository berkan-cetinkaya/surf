<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modal - SURF Example</title>
    <style>
        :root {
            --glass-bg: rgba(30, 41, 59, 0.4);
            --glass-border: rgba(255, 255, 255, 0.08);
            --accent-primary: #38bdf8;
            --accent-secondary: #818cf8;
            --text-main: #f8fafc;
            --text-dim: #94a3b8;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: radial-gradient(circle at top right, #1e1b4b, #0f172a);
            background-attachment: fixed;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--text-main);
            padding: 1.5rem;
        }

        .container {
            text-align: center;
            width: 100%;
            max-width: 500px;
        }

        h1 {
            font-size: clamp(1.5rem, 4vw, 2.2rem);
            font-weight: 800;
            margin-bottom: 2.5rem;
            background: linear-gradient(135deg, #fff 0%, var(--accent-primary) 100%);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: -0.04em;
        }

        .demo-area {
            background: var(--glass-bg);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border: 1px solid var(--glass-border);
            border-radius: 32px;
            padding: 4rem 2rem;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
            position: relative;
        }

        .open-btn {
            background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));
            color: #0f172a;
            border: none;
            padding: 1rem 2.5rem;
            border-radius: 16px;
            font-size: 1.1rem;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s cubic-bezier(0.23, 1, 0.32, 1);
            box-shadow: 0 10px 25px -5px rgba(56, 189, 248, 0.3);
        }

        .open-btn:hover {
            transform: translateY(-4px) scale(1.02);
            box-shadow: 0 20px 35px -10px rgba(56, 189, 248, 0.4);
            filter: brightness(1.1);
        }

        .modal-overlay {
            position: fixed;
            inset: 0;
            background: rgba(15, 23, 42, 0.8);
            backdrop-filter: blur(8px);
            -webkit-backdrop-filter: blur(8px);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 100;
            padding: 1.5rem;
        }

        .modal {
            background: #1e293b;
            border: 1px solid var(--glass-border);
            border-radius: 28px;
            padding: 2.5rem;
            max-width: 440px;
            width: 100%;
            box-shadow: 0 30px 60px -12px rgba(0, 0, 0, 0.6);
            animation: modalIn 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
            position: relative;
        }

        @keyframes modalIn {
            from {
                opacity: 0;
                transform: scale(0.9) translateY(20px);
            }

            to {
                opacity: 1;
                transform: scale(1) translateY(0);
            }
        }

        .modal h2 {
            font-size: 1.75rem;
            font-weight: 800;
            margin-bottom: 1rem;
            color: var(--accent-primary);
            letter-spacing: -0.02em;
        }

        .modal p {
            color: var(--text-dim);
            margin-bottom: 2rem;
            line-height: 1.7;
            font-size: 1.05rem;
        }

        .modal-actions {
            display: flex;
            gap: 1rem;
            justify-content: stretch;
        }

        .modal-actions button {
            flex: 1;
            padding: 0.85rem 1.5rem;
            border-radius: 14px;
            border: 1px solid transparent;
            cursor: pointer;
            font-weight: 700;
            font-size: 0.95rem;
            transition: all 0.2s;
        }

        .modal .cancel {
            background: rgba(255, 255, 255, 0.05);
            color: var(--text-dim);
            border-color: var(--glass-border);
        }

        .modal .cancel:hover {
            background: rgba(255, 255, 255, 0.1);
            color: var(--text-main);
        }

        .modal .confirm {
            background: var(--accent-primary);
            color: #0f172a;
        }

        .modal .confirm:hover {
            filter: brightness(1.1);
            transform: translateY(-2px);
        }

        .back {
            position: fixed;
            top: 2rem;
            left: 2rem;
            color: var(--text-dim);
            text-decoration: none;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            transition: all 0.2s;
            z-index: 10;
        }

        .back:hover {
            color: var(--accent-primary);
            transform: translateX(-5px);
        }

        .code-reveal {
            margin-top: 3rem;
            text-align: left;
        }

        .code-label {
            display: block;
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            color: var(--text-dim);
            margin-bottom: 0.75rem;
            font-weight: 700;
            padding-left: 0.5rem;
        }

        code {
            display: block;
            background: rgba(0, 0, 0, 0.3);
            padding: 1.25rem;
            border-radius: 16px;
            font-size: 0.85rem;
            color: var(--accent-primary);
            font-family: 'JetBrains Mono', 'SF Mono', monospace;
            line-height: 1.6;
            border: 1px solid var(--glass-border);
            overflow-x: auto;
        }
    </style>
    <%- include('../../partials/core-scripts') %>
</head>

<body>
    <a href="/showcase/01-interaction" class="back" d-pulse="navigate">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"
            stroke-linecap="round" stroke-linejoin="round">
            <line x1="19" y1="12" x2="5" y2="12"></line>
            <polyline points="12 19 5 12 12 5"></polyline>
        </svg>
        Back
    </a>

    <div class="container" d-cell="{ open: false }" d-id="toggle-container">
        <h1>Modal Example</h1>

        <div class="demo-area">
            <button class="open-btn" d-signal="click: open = true">
                Launch Experience
            </button>
        </div>

        <div class="code-reveal">
            <span class="code-label">Implementation</span>
            <code>
&lt;div d-cell="{ open: false }"&gt;
  &lt;button d-signal="click: open = true"&gt;Open&lt;/button&gt;
  &lt;div d-show="open"&gt;
    &lt;button d-signal="click: open = false"&gt;Close&lt;/button&gt;
  &lt;/div&gt;
&lt;/div&gt;
            </code>
        </div>

        <!-- Modal with d-show, outside demo-area to avoid stacking context issues -->
        <div class="modal-overlay" d-show="open" style="display: none;">
            <div class="modal">
                <h2>Premium Interaction</h2>
                <p>This modal is powered by Surf's local state management. Seamless, fast, and completely
                    client-side.</p>

                <div class="modal-actions">
                    <button class="cancel" d-signal="click: open = false">Dismiss</button>
                    <button class="confirm" d-signal="click: open = false">Acknowledged</button>
                </div>
            </div>
        </div>
    </div>

</body>

</html>