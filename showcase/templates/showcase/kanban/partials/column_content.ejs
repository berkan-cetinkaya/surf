<div class="column-header">
    <div class="header-title">
        <% if (column.id==='todo' ) { %><span class="dot-indicator todo"></span>
            <% } %>
                <% if (column.id==='doing' ) { %><span class="dot-indicator doing"></span>
                    <% } %>
                        <% if (column.id==='done' ) { %><span class="dot-indicator done"></span>
                            <% } %>
                                <%= column.title %>
    </div>
    <span class="column-count">
        <%= column.tasks.length %>
    </span>
</div>

<div class="task-list" d-drop-zone="true" d-drop-url="/showcase/kanban/move"
    d-drop-data='{"toCol": "<%= column.id %>"<%= (typeof activeSprintId !== "undefined" && activeSprintId) ? `, "toSprintId": "${activeSprintId}"` : "" %>}'>
    <% column.tasks.forEach(task=> { %>
        <%- include('card.ejs', { task, currentColumnId: column.id }) %>
            <% }); %>
</div>

<% if (column.id==='todo' ) { %>
    <form class="add-task-form" d-pulse="commit" method="POST" d-target="#col-<%= column.id %>"
        action="/showcase/kanban/add" d-swap="replace" d-cell="{ editing: false }"
        d-id="add-task-form-<%= column.id %>">
        <input type="hidden" name="columnId" value="<%= column.id %>">
        <% if (typeof activeSprintId !=='undefined' && activeSprintId) { %>
            <input type="hidden" name="sprintId" value="<%= activeSprintId %>">
            <% } %>

                <!-- Toggle Button -->
                <button type="button" class="ghost-add-btn" d-show="!editing" d-signal="click: editing = true">
                    + New Task
                </button>

                <!-- Input Group -->
                <div class="input-group" d-show="editing" style="gap: 0.5rem; width: 100%;">
                    <input type="text" name="title" class="add-input" placeholder="Enter task title..." required
                        d-signal="blur: if(this.value === '') editing = false; keydown: if(event.key === 'Escape') editing = false">

                    <!-- Explicit Submit Button -->
                    <button type="submit" class="submit-btn-mini" title="Add Task">↵</button>
                </div>
    </form>
    <style>
        .input-group {
            align-items: center;
            animation: fadeIn 0.2s ease-out;
        }

        .submit-btn-mini {
            background: var(--brand-primary);
            color: white;
            border: none;
            border-radius: 6px;
            width: 32px;
            height: 32px;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            font-weight: bold;
        }

        .submit-btn-mini:hover {
            filter: brightness(1.1);
        }
    </style>
    <% } %>