<aside class="dashboard-sidebar" d-attr="class.open: sidebarOpen">
    <div class="sidebar-header-mobile">
        <span class="logo-text">Surf Board</span>
        <button class="close-sidebar" d-signal="click: sidebarOpen = false" aria-label="Close Menu">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"
                stroke-linecap="round" stroke-linejoin="round">
                <line x1="18" y1="6" x2="6" y2="18"></line>
                <line x1="6" y1="6" x2="18" y2="18"></line>
            </svg>
        </button>
    </div>

    <a href="/" d-pulse="navigate" class="nav-logo" d-signal="click: sidebarOpen = false">
        <img src="/brand/surf-logo.png" alt="SURF">
        <span class="logo-text">Surf</span>
        <span class="beta-tag">Beta</span>
        <span class="version-tag">v0.3.1</span>
    </a>

    <nav class="sidebar-nav">
        <!-- Dashboard Button -->
        <div class="nav-item">
            <a href="/showcase/kanban" d-pulse="navigate" d-target="body"
                style="text-decoration: none; color: inherit; display: flex; align-items: center; gap: 0.75rem;">
                <span class="icon">📊</span>
                Dashboard
            </a>
        </div>

        <!-- Backlog Button -->
        <div class="nav-item">
            <a href="/showcase/kanban/backlog" d-pulse="navigate" d-target="body"
                style="text-decoration: none; color: inherit; display: flex; align-items: center; gap: 0.75rem;">
                <span class="icon">📝</span>
                Backlog
            </a>
        </div>
    </nav>

    <div class="sidebar-footer">
        <div class="status-indicator">
            <div class="pulse-dot"></div>
            <p class="engine-status">Running <span class="version">v0.3.1</span>. Server-driven reactivity is active.
            </p>
        </div>
    </div>
</aside>

<style>
    /* Sidebar Styles */
    .dashboard-sidebar {
        width: 260px;
        background: rgba(15, 23, 42, 0.4);
        border-right: 1px solid var(--border-subtle);
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
        display: flex;
        flex-direction: column;
        padding: 1.25rem 1.75rem;
        /* Matches Header padding-top */
        gap: 1.5rem;
        height: 100vh;
        position: sticky;
        top: 0;
    }

    .sidebar-header-mobile {
        display: none;
        /* Hidden on desktop */
    }

    .nav-logo {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        text-decoration: none;
        transition: opacity 0.2s;
        margin-bottom: 2rem;
    }

    .nav-logo:hover {
        opacity: 0.8;
    }

    .group-title {
        font-size: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        color: var(--text-muted);
        margin-bottom: 0.75rem;
        font-weight: 600;
        padding-left: 0.75rem;
    }

    .nav-menu {
        display: flex;
        flex-direction: column;
        gap: 0.25rem;
    }

    .nav-item {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding: 0.6rem 0.75rem;
        color: var(--text-muted);
        text-decoration: none;
        border-radius: 6px;
        font-size: 0.9rem;
        font-weight: 500;
        transition: all 0.2s;
    }

    .nav-item:hover {
        background: rgba(255, 255, 255, 0.05);
        color: var(--text-main);
    }

    .nav-item.active {
        background: rgba(56, 189, 248, 0.12);
        color: var(--brand-primary);
        box-shadow: inset 0 0 12px rgba(56, 189, 248, 0.05);
    }

    .nav-item .icon {
        font-size: 1.1rem;
        width: 20px;
        text-align: center;
    }

    .dot {
        width: 8px;
        height: 8px;
        border-radius: 50%;
    }

    .dot.success {
        background: var(--success);
        box-shadow: 0 0 8px var(--success);
    }

    .dot.info {
        background: var(--brand-primary);
        box-shadow: 0 0 8px var(--brand-primary);
    }

    .dot.warning {
        background: var(--danger);
        box-shadow: 0 0 8px var(--danger);
    }

    .sidebar-header-mobile {
        display: none;
        align-items: center;
        justify-content: space-between;
        margin-bottom: 2rem;
    }

    .logo-text {
        font-weight: 800;
        font-size: 1.25rem;
        background: linear-gradient(135deg, #fff, var(--brand-primary));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    @media (max-width: 768px) {
        .sidebar-header-mobile {
            display: flex;
        }

        .nav-logo {
            display: none;
        }
    }

    /* Duplicate nav-logo removed */

    .nav-logo img {
        height: 28px;
        width: 28px;
        border-radius: 6px;
        box-shadow: 0 4px 12px rgba(56, 189, 248, 0.3);
    }

    .nav-logo .logo-text {
        font-size: 1.1rem;
        font-weight: 800;
        color: var(--text-main);
        letter-spacing: -0.03em;
    }

    .nav-logo .beta-tag {
        font-size: 0.6rem;
        background: var(--brand-secondary);
        color: #fff;
        padding: 0.1rem 0.4rem;
        border-radius: 4px;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        font-weight: 700;
    }

    .nav-logo .version-tag {
        font-size: 0.65rem;
        color: var(--text-muted);
        font-family: monospace;
        opacity: 0.7;
    }

    /* Sidebar Footer */
    .sidebar-footer {
        margin-top: auto;
    }

    .engine-card {
        background: rgba(30, 41, 59, 0.5);
        border-radius: 8px;
        padding: 1rem;
        border: 1px solid var(--border-subtle);
    }

    .engine-header {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        margin-bottom: 0.5rem;
    }

    .flash-icon {
        color: var(--brand-primary);
    }

    .engine-title {
        font-weight: 700;
        font-size: 0.85rem;
        background: linear-gradient(135deg, #fff, var(--brand-primary));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .engine-status {
        font-size: 0.75rem;
        color: var(--text-muted);
        line-height: 1.4;
    }

    .version {
        color: var(--brand-secondary);
        font-family: monospace;
    }
</style>