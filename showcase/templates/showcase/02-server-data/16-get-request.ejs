<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>News Feed - SURF Example</title>
    <style>
        :root {
            --glass-bg: rgba(30, 41, 59, 0.4);
            --glass-border: rgba(255, 255, 255, 0.08);
            --accent-primary: #38bdf8;
            --accent-secondary: #818cf8;
            --text-main: #f8fafc;
            --text-dim: #94a3b8;
            --danger: #f43f5e;
            --success: #10b981;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: radial-gradient(circle at top right, #1e1b4b, #0f172a);
            background-attachment: fixed;
            min-height: 100vh;
            color: var(--text-main);
            padding: 4rem 1.5rem;
        }

        .container {
            max-width: 650px;
            margin: 0 auto;
        }

        h1 {
            font-size: clamp(1.5rem, 4vw, 2.2rem);
            font-weight: 800;
            margin-bottom: 0.5rem;
            background: linear-gradient(135deg, #fff 0%, var(--accent-primary) 100%);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: -0.04em;
            text-align: center;
        }

        .subtitle {
            color: var(--text-dim);
            margin-bottom: 2.5rem;
            font-size: 1rem;
            text-align: center;
            font-weight: 500;
        }

        .explanation {
            background: rgba(56, 189, 248, 0.05);
            border: 1px solid var(--glass-border);
            border-radius: 20px;
            padding: 1.5rem;
            margin-bottom: 2.5rem;
            font-size: 0.95rem;
            color: var(--text-dim);
            line-height: 1.6;
        }

        .explanation strong {
            color: var(--accent-primary);
        }

        .explanation ol {
            margin-left: 1.25rem;
            margin-top: 0.75rem;
        }

        .status {
            text-align: center;
            margin-bottom: 1.5rem;
            font-size: 0.8rem;
            color: var(--text-dim);
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
        }

        .status::before {
            content: "";
            width: 8px;
            height: 8px;
            background: var(--success);
            border-radius: 50%;
            display: inline-block;
            box-shadow: 0 0 10px var(--success);
            animation: pulse-dot 2s infinite;
        }

        @keyframes pulse-dot {
            0% {
                transform: scale(0.95);
                opacity: 0.5;
            }

            50% {
                transform: scale(1.1);
                opacity: 1;
            }

            100% {
                transform: scale(0.95);
                opacity: 0.5;
            }
        }

        /* News Feed Styles */
        .news-feed {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .feed-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 0.5rem 1rem;
            border-bottom: 1px solid var(--glass-border);
            margin-bottom: 0.5rem;
            font-weight: 700;
            font-size: 0.9rem;
            color: var(--text-dim);
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        .news-item {
            background: var(--glass-bg);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border: 1px solid var(--glass-border);
            border-radius: 20px;
            padding: 1.5rem;
            transition: all 0.3s cubic-bezier(0.23, 1, 0.32, 1);
            cursor: pointer;
            position: relative;
            overflow: hidden;
        }

        .news-item:hover {
            transform: translateY(-4px);
            background: rgba(255, 255, 255, 0.08);
            border-color: rgba(56, 189, 248, 0.3);
            box-shadow: 0 20px 40px -10px rgba(0, 0, 0, 0.3);
        }

        .news-item.breaking {
            border-left: 4px solid var(--danger);
            background: rgba(244, 63, 94, 0.05);
        }

        .news-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 0.75rem;
        }

        .category {
            font-size: 0.7rem;
            text-transform: uppercase;
            font-weight: 800;
            background: rgba(56, 189, 248, 0.1);
            color: var(--accent-primary);
            padding: 0.25rem 0.6rem;
            border-radius: 6px;
            letter-spacing: 0.05em;
        }

        .category.breaking {
            background: rgba(244, 63, 94, 0.1);
            color: var(--danger);
        }

        .time {
            font-size: 0.75rem;
            color: var(--text-dim);
        }

        .news-title {
            font-size: 1.15rem;
            font-weight: 700;
            color: var(--text-main);
            line-height: 1.4;
            transition: color 0.2s;
        }

        .news-item:hover .news-title {
            color: var(--accent-primary);
        }

        .read-indicator {
            margin-top: 1rem;
            font-size: 0.75rem;
            font-weight: 700;
            color: var(--success);
            display: flex;
            align-items: center;
            gap: 0.4rem;
        }

        .loading {
            text-align: center;
            padding: 4rem 2rem;
            color: var(--text-dim);
            font-style: italic;
        }

        .back {
            position: fixed;
            top: 2rem;
            left: 2rem;
            color: var(--text-dim);
            text-decoration: none;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            transition: all 0.2s;
            z-index: 10;
        }

        .back:hover {
            color: var(--accent-primary);
            transform: translateX(-5px);
        }

        .news-item[data-read="true"] {
            opacity: 0.5;
            filter: grayscale(0.5);
        }
    </style>
    <%- include('../../partials/core-scripts') %>
</head>

<body>
    <a href="/showcase/02-server-data" class="back" d-pulse="navigate">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"
            stroke-linecap="round" stroke-linejoin="round">
            <line x1="19" y1="12" x2="5" y2="12"></line>
            <polyline points="12 19 5 12 12 5"></polyline>
        </svg>
        Back
    </a>

    <div class="container">
        <h1>Live News Feed</h1>
        <p class="subtitle">Automatic Surface Synchronization</p>

        <div class="explanation">
            <strong>🔑 Architecture Summary:</strong> Click any story to mark as read.
            <br>
            <ol>
                <li><code>d-auto-refresh</code> polls <code>/api/news</code> every 5 seconds.</li>
                <li><code>d-pulse="action"</code> sends a seamless update on click.</li>
                <li>State is preserved across refreshes via Echo.</li>
            </ol>
        </div>

        <div class="status" id="status">Syncing with server...</div>

        <!-- Surface with auto-refresh -->
        <div id="news-surface" d-auto-refresh="5000" d-auto-refresh-url="/api/news">
            <div class="loading">Establishing secure connection to news feed...</div>
        </div>
    </div>

        <script>
            // Minimal JS: update status on patch
            if (window.Surf) {
                Surf.on('pulse:end', () => {
                    const status = document.getElementById('status');
                    if (status) {
                        status.textContent = 'Last synced at ' + new Date().toLocaleTimeString();
                    }
                });
            }
        </script>
</body>

</html>