<!DOCTYPE html>
<html lang="en">

<head>
    <title>
        <%= title %> - SURF Showcase
    </title>
    <%- include('../partials/head') %>
        <style>
            :root {
                --glass-bg: rgba(30, 41, 59, 0.4);
                --glass-border: rgba(255, 255, 255, 0.08);
                --accent-primary: #38bdf8;
                --accent-secondary: #818cf8;
                --text-main: #f8fafc;
                --text-dim: #94a3b8;
            }

            .category-header {
                margin-bottom: 3rem;
                padding-top: 1rem;
            }

            .category-title {
                font-size: clamp(1.8rem, 5vw, 2.8rem);
                font-weight: 800;
                margin-bottom: 0.75rem;
                display: flex;
                align-items: center;
                gap: 1rem;
                line-height: 1.1;
                letter-spacing: -0.04em;
            }

            .category-title-text {
                background: linear-gradient(135deg, #fff 0%, var(--accent-primary) 100%);
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
                background-clip: text;
                filter: drop-shadow(0 2px 10px rgba(56, 189, 248, 0.2));
            }

            .category-title-emoji {
                font-size: 1.2em;
                filter: drop-shadow(0 0 15px rgba(255, 255, 255, 0.2));
            }

            .category-subtitle {
                color: var(--text-dim);
                font-size: clamp(0.95rem, 2.5vw, 1.15rem);
                max-width: 600px;
                line-height: 1.6;
            }

            .item-grid {
                display: grid;
                grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
                gap: 2rem;
                margin-top: 1rem;
            }

            .item-card {
                background: var(--glass-bg);
                backdrop-filter: blur(12px);
                -webkit-backdrop-filter: blur(12px);
                border: 1px solid var(--glass-border);
                border-radius: 24px;
                padding: 2rem;
                text-decoration: none;
                transition: all 0.4s cubic-bezier(0.23, 1, 0.32, 1);
                display: flex;
                flex-direction: column;
                gap: 1.5rem;
                position: relative;
                overflow: hidden;
            }

            .item-card::after {
                content: "";
                position: absolute;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                background: radial-gradient(circle at top right, rgba(56, 189, 248, 0.1), transparent 70%);
                opacity: 0;
                transition: opacity 0.4s ease;
            }

            .item-card:hover {
                transform: translateY(-8px) scale(1.02);
                border-color: rgba(56, 189, 248, 0.4);
                background: rgba(30, 41, 59, 0.6);
                box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4), 0 0 20px rgba(56, 189, 248, 0.1);
            }

            .item-card:hover::after {
                opacity: 1;
            }

            .item-card-header {
                display: flex;
                justify-content: space-between;
                align-items: flex-start;
                position: relative;
                z-index: 2;
            }

            .item-icon {
                font-size: 2.2rem;
                width: 60px;
                height: 60px;
                display: flex;
                align-items: center;
                justify-content: center;
                background: rgba(255, 255, 255, 0.03);
                border: 1px solid rgba(255, 255, 255, 0.05);
                border-radius: 18px;
                transition: transform 0.3s ease;
            }

            .item-card:hover .item-icon {
                transform: rotate(-5deg) scale(1.1);
                background: rgba(56, 189, 248, 0.1);
                border-color: rgba(56, 189, 248, 0.2);
            }

            .item-card-actions {
                display: flex;
                flex-direction: column;
                align-items: flex-end;
                gap: 0.75rem;
            }

            .item-badge {
                padding: 0.3rem 0.8rem;
                background: rgba(56, 189, 248, 0.1);
                color: var(--accent-primary);
                border-radius: 100px;
                font-size: 0.7rem;
                font-family: 'JetBrains Mono', 'SF Mono', monospace;
                font-weight: 700;
                letter-spacing: 0.05em;
                border: 1px solid rgba(56, 189, 248, 0.2);
                text-transform: uppercase;
            }

            .detail-icon-wrapper {
                color: var(--text-dim);
                opacity: 0.4;
                transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
                padding: 8px;
                border-radius: 12px;
                background: rgba(255, 255, 255, 0.03);
                display: flex;
                align-items: center;
                justify-content: center;
            }

            .item-card:hover .detail-icon-wrapper {
                opacity: 1;
                color: var(--text-main);
            }

            .detail-icon-wrapper:hover {
                background: var(--accent-primary);
                color: #0f172a !important;
                transform: scale(1.15) rotate(10deg);
            }

            .item-info {
                position: relative;
                z-index: 2;
            }

            .item-info h3 {
                color: var(--text-main);
                font-size: 1.4rem;
                font-weight: 700;
                margin-bottom: 0.5rem;
                letter-spacing: -0.02em;
            }

            .item-info p {
                color: var(--text-dim);
                font-size: 0.95rem;
                line-height: 1.7;
                margin-bottom: 1rem;
                display: -webkit-box;
                line-clamp: 3;
                -webkit-line-clamp: 3;
                -webkit-box-orient: vertical;
                overflow: hidden;
            }

            .item-footer {
                margin-top: auto;
                display: flex;
                align-items: center;
                justify-content: space-between;
                color: var(--accent-primary);
                font-weight: 700;
                font-size: 0.9rem;
                overflow: hidden;
                position: relative;
                z-index: 2;
            }

            .explore-text {
                display: flex;
                align-items: center;
                gap: 0.6rem;
                transition: transform 0.3s ease;
            }

            .item-card:hover .explore-text {
                transform: translateX(5px);
            }

            .empty-state {
                padding: 6rem 2rem;
                text-align: center;
                background: var(--glass-bg);
                border: 2px dashed var(--glass-border);
                border-radius: 32px;
                color: var(--text-dim);
            }

            .empty-state-icon {
                font-size: 4rem;
                margin-bottom: 2rem;
                display: block;
                filter: grayscale(1) opacity(0.5);
            }

            @media (max-width: 768px) {
                .category-header {
                    margin-bottom: 2.5rem;
                }

                .category-title {
                    gap: 0.75rem;
                }

                .category-title-emoji {
                    font-size: 1.1em;
                }

                .item-grid {
                    grid-template-columns: 1fr;
                    gap: 1.25rem;
                }

                .item-card {
                    padding: 1.75rem;
                    gap: 1.25rem;
                }

                .item-info h3 {
                    font-size: 1.25rem;
                }

                .app-sidebar {
                    position: fixed;
                    top: 0;
                    left: 0;
                    bottom: 0;
                    width: 280px;
                    z-index: 1001;
                    background: #0f172a;
                    transform: translateX(-100%);
                    transition: transform 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
                    box-shadow: 4px 0 24px rgba(0, 0, 0, 0.5);
                    display: flex !important;
                    flex-direction: column;
                    border-right: 1px solid rgba(255, 255, 255, 0.05);
                }

                .app-sidebar.open {
                    transform: translateX(0);
                }

                .mobile-overlay {
                    position: fixed;
                    top: 0;
                    left: 0;
                    right: 0;
                    bottom: 0;
                    background: rgba(0, 0, 0, 0.7);
                    backdrop-filter: blur(8px);
                    -webkit-backdrop-filter: blur(8px);
                    z-index: 1000;
                    opacity: 0;
                    visibility: hidden;
                    transition: all 0.3s ease;
                }

                .mobile-overlay.open {
                    opacity: 1;
                    visibility: visible;
                }
            }

            .app-layout {
                display: grid;
                grid-template-columns: 260px 1fr;
                min-height: 100vh;
                background: radial-gradient(circle at top right, #1e1b4b, #0f172a);
                background-attachment: fixed;
            }

            .app-main {
                padding: 2.5rem;
                max-width: 1400px;
                margin: 0 auto;
                width: 100%;
            }

            @media (max-width: 768px) {
                .app-layout {
                    grid-template-columns: 1fr;
                }

                .app-main {
                    padding: 1.5rem;
                }
            }
        </style>
</head>

<body d-cell="{ sidebarOpen: false }">
    <div class="mobile-overlay" d-attr="class.open: sidebarOpen" d-signal="click: sidebarOpen = false"></div>
    <div class="app-layout" d-id="layout">
        <%- include('../partials/header', { breadcrumbs: [{ label: 'Showcase' , href: '/showcase' }, { label: title }]
            }) %>

            <%- include('../partials/sidebar', { activeCategory: activeCategory, activeExample: null }) %>

                <main class="app-main">
                    <header class="category-header">
                        <h2 class="category-title">
                            <span class="category-title-emoji">
                                <%= emoji %>
                            </span>
                            <span class="category-title-text">
                                <%= title %>
                            </span>
                        </h2>
                        <p class="category-subtitle">
                            Discover <%= items.length %> interactive building block<%= items.length !==1 ? 's' : '' %>
                                    for this category.
                        </p>
                    </header>

                    <% if (items.length===0) { %>
                        <div class="empty-state">
                            <span class="empty-state-icon">🚧</span>
                            <h3>Showcase items coming soon</h3>
                            <p>This category is currently under active development.</p>
                        </div>
                        <% } else { %>
                            <div class="item-grid">
                                <% items.forEach(item=> { %>
                                    <a href="/showcase/<%= activeCategory %>/<%= item.id %>" class="item-card">
                                        <div class="item-card-header">
                                            <div class="item-icon">🧩</div>
                                            <div class="item-card-actions">
                                                <span class="item-badge">#<%= item.id.split('-')[0] %></span>
                                                <div class="detail-icon-wrapper">
                                                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none"
                                                        stroke="currentColor" stroke-width="2.5">
                                                        <path
                                                            d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6">
                                                        </path>
                                                        <polyline points="15 3 21 3 21 9"></polyline>
                                                        <line x1="10" y1="14" x2="21" y2="3"></line>
                                                    </svg>
                                                </div>
                                            </div>
                                        </div>
                                        <div class="item-info">
                                            <h3>
                                                <%= item.title %>
                                            </h3>
                                            <p>Interactive demonstration of <%= item.title.toLowerCase() %> concepts
                                                    using SURF signals and cells.</p>
                                        </div>
                                        <div class="item-footer">
                                            <div class="explore-text">
                                                <span>Explore Component</span>
                                                <svg width="16" height="16" viewBox="0 0 24 24" fill="none"
                                                    stroke="currentColor" stroke-width="2.5" stroke-linecap="round"
                                                    stroke-linejoin="round">
                                                    <line x1="5" y1="12" x2="19" y2="12"></line>
                                                    <polyline points="12 5 19 12 12 19"></polyline>
                                                </svg>
                                            </div>
                                        </div>
                                    </a>
                                    <% }) %>
                            </div>
                            <% } %>
                </main>
    </div>

    <%- include('../partials/core-scripts') %>
</body>

</html>