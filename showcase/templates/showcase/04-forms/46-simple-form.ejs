<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Form - SURF Example</title>
    <style>
        :root {
            --glass-bg: rgba(30, 41, 59, 0.4);
            --glass-border: rgba(255, 255, 255, 0.08);
            --accent-primary: #38bdf8;
            --accent-secondary: #818cf8;
            --text-main: #f8fafc;
            --text-dim: #94a3b8;
            --danger: #f43f5e;
            --success: #10b981;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: radial-gradient(circle at top right, #1e1b4b, #0f172a);
            background-attachment: fixed;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--text-main);
            padding: 1.5rem;
        }

        .container {
            width: 100%;
            max-width: 500px;
            animation: fadeIn 0.6s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        h1 {
            font-size: clamp(1.5rem, 4vw, 2.2rem);
            font-weight: 800;
            margin-bottom: 0.5rem;
            background: linear-gradient(135deg, #fff 0%, var(--accent-primary) 100%);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: -0.04em;
            text-align: center;
        }

        .subtitle {
            color: var(--text-dim);
            margin-bottom: 2.5rem;
            font-size: 1rem;
            text-align: center;
            font-weight: 500;
        }

        .form-container {
            background: var(--glass-bg);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border: 1px solid var(--glass-border);
            border-radius: 32px;
            padding: 2.5rem;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
        }

        form {
            display: flex;
            flex-direction: column;
            gap: 1.25rem;
        }

        .field {
            text-align: left;
        }

        label {
            display: block;
            margin-bottom: 0.6rem;
            color: var(--text-dim);
            font-size: 0.85rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            padding-left: 0.5rem;
        }

        input {
            width: 100%;
            padding: 0.85rem 1.25rem;
            border-radius: 16px;
            border: 1px solid var(--glass-border);
            background: rgba(0, 0, 0, 0.2);
            color: var(--text-main);
            font-size: 1rem;
            transition: all 0.3s cubic-bezier(0.23, 1, 0.32, 1);
            font-family: inherit;
        }

        input:focus {
            outline: none;
            border-color: var(--accent-primary);
            background: rgba(0, 0, 0, 0.3);
            box-shadow: 0 0 0 4px rgba(56, 189, 248, 0.1);
        }

        input.error {
            border-color: var(--danger);
            background: rgba(244, 63, 94, 0.05);
        }

        .error-message {
            color: var(--danger);
            font-size: 0.8rem;
            font-weight: 600;
            margin-top: 0.5rem;
            padding-left: 0.5rem;
            display: flex;
            align-items: center;
            gap: 0.4rem;
        }

        .error-message::before {
            content: "⚠️";
            font-size: 0.9rem;
        }

        .success-card {
            text-align: center;
            padding: 1rem 0;
        }

        .success-icon {
            width: 64px;
            height: 64px;
            background: rgba(16, 185, 129, 0.1);
            color: var(--success);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
            font-size: 2rem;
            border: 1px solid rgba(16, 185, 129, 0.2);
        }

        .success-message h3 {
            color: var(--success);
            font-size: 1.5rem;
            font-weight: 800;
            margin-bottom: 0.75rem;
            letter-spacing: -0.02em;
        }

        .success-message p {
            color: var(--text-dim);
            line-height: 1.6;
            margin-bottom: 2rem;
        }

        button {
            background: linear-gradient(135deg, var(--accent-secondary), var(--accent-primary));
            color: #0f172a;
            border: none;
            padding: 1rem;
            border-radius: 16px;
            font-size: 1rem;
            font-weight: 800;
            cursor: pointer;
            transition: all 0.3s cubic-bezier(0.23, 1, 0.32, 1);
            width: 100%;
            margin-top: 0.5rem;
            box-shadow: 0 10px 20px -5px rgba(56, 189, 248, 0.3);
        }

        button:hover {
            transform: translateY(-2px);
            filter: brightness(1.1);
            box-shadow: 0 15px 30px -10px rgba(56, 189, 248, 0.5);
        }

        button:active {
            transform: translateY(0);
        }

        .btn-ghost {
            background: rgba(255, 255, 255, 0.05);
            color: var(--text-main);
            box-shadow: none;
            border: 1px solid var(--glass-border);
        }

        .btn-ghost:hover {
            background: rgba(255, 255, 255, 0.1);
            box-shadow: none;
        }

        .back {
            position: fixed;
            top: 2rem;
            left: 2rem;
            color: var(--text-dim);
            text-decoration: none;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            transition: all 0.2s;
            z-index: 10;
        }

        .back:hover {
            color: var(--accent-primary);
            transform: translateX(-5px);
        }

        .note {
            margin-top: 3rem;
            background: rgba(56, 189, 248, 0.05);
            border: 1px solid var(--glass-border);
            padding: 1.5rem;
            border-radius: 20px;
            color: var(--accent-primary);
            font-size: 0.9rem;
            text-align: left;
            line-height: 1.6;
        }

        .note strong {
            display: block;
            margin-bottom: 0.25rem;
        }
    </style>
</head>

<body>
    <a href="/showcase/04-forms" class="back" d-pulse="navigate">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"
            stroke-linecap="round" stroke-linejoin="round">
            <line x1="19" y1="12" x2="5" y2="12"></line>
            <polyline points="12 19 5 12 12 5"></polyline>
        </svg>
        Back
    </a>

    <div class="container">
        <h1>Secure Validation</h1>
        <p class="subtitle">Seamless Server-Optimized Forms</p>

        <!-- Surface for form updates -->
        <div id="form-surface" class="form-container">
            <form action="/api/form" method="POST" d-pulse="commit" d-target="#form-surface">
                <div class="field">
                    <label for="name">Full Name</label>
                    <input type="text" id="name" name="name" placeholder="John Doe">
                </div>

                <div class="field">
                    <label for="email">Work Email</label>
                    <input type="email" id="email" name="email" placeholder="john@example.com">
                </div>

                <div class="field">
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" placeholder="••••••••">
                </div>

                <button type="submit">Complete Registration</button>
            </form>
        </div>

        <div class="note">
            <strong>Declarative Flow:</strong> <code>d-pulse="commit"</code> handles the AJAX submission and patch
            application automatically. Try submitting with empty fields to trigger real-time server-side validation.
        </div>
    </div>

    <%- include('../../partials/core-scripts') %>
</body>

</html>