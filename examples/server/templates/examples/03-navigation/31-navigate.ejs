<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Navigation - SURF Example</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            min-height: 100vh;
            color: #eee;
        }

        .layout {
            display: flex;
            min-height: 100vh;
        }

        nav {
            width: 200px;
            background: rgba(0, 0, 0, 0.3);
            padding: 1rem;
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
        }

        nav .logo {
            font-size: 1.5rem;
            font-weight: bold;
            color: #00d4ff;
            margin-bottom: 1rem;
            padding: 0.5rem;
        }

        nav a {
            color: #888;
            text-decoration: none;
            padding: 0.75rem 1rem;
            border-radius: 8px;
            transition: all 0.2s;
        }

        nav a:hover {
            background: rgba(255, 255, 255, 0.1);
            color: #fff;
        }

        nav a.active {
            background: linear-gradient(90deg, rgba(124, 58, 237, 0.3), rgba(0, 212, 255, 0.3));
            color: #00d4ff;
        }

        main {
            flex: 1;
            padding: 2rem;
        }

        .content {
            max-width: 600px;
        }

        .content h1 {
            color: #00d4ff;
            margin-bottom: 1rem;
        }

        .content p {
            line-height: 1.8;
            margin-bottom: 1rem;
            color: #aaa;
        }

        .back {
            position: fixed;
            top: 1rem;
            right: 1rem;
            color: #888;
            text-decoration: none;
        }

        .back:hover {
            color: #fff;
        }

        .note {
            margin-top: 2rem;
            background: rgba(124, 58, 237, 0.1);
            border: 1px solid rgba(124, 58, 237, 0.2);
            padding: 1rem;
            border-radius: 8px;
            color: #a78bfa;
            font-size: 0.85rem;
        }
    </style>
</head>

<body>
    <a href="/examples/03-navigation" class="back">← Back</a>

    <!-- Nav is a Cell with currentPage state -->
    <div class="layout" d-cell="{currentPage: 'home'}" d-id="nav">
        <nav>
            <div class="logo">SURF</div>

            <!-- Navigation with d-pulse, d-signal and d-attr for active state -->
            <a href="/page/home" d-pulse="navigate" d-target="#main" d-signal="click: currentPage = 'home'"
                d-attr="class.active: currentPage === 'home'">
                🏠 Home
            </a>
            <a href="/page/about" d-pulse="navigate" d-target="#main" d-signal="click: currentPage = 'about'"
                d-attr="class.active: currentPage === 'about'">
                ℹ️ About
            </a>
            <a href="/page/products" d-pulse="navigate" d-target="#main" d-signal="click: currentPage = 'products'"
                d-attr="class.active: currentPage === 'products'">
                📦 Products
            </a>
            <a href="/page/contact" d-pulse="navigate" d-target="#main" d-signal="click: currentPage = 'contact'"
                d-attr="class.active: currentPage === 'contact'">
                ✉️ Contact
            </a>
        </nav>

        <!-- Surface that gets replaced -->
        <main id="main">
            <div class="content">
                <h1>🏠 Home</h1>
                <p>
                    Welcome to the SURF navigation example. Click the links in the sidebar
                    to see how content updates without a full page reload.
                </p>
                <p>
                    Each link fetches real HTML from the server and replaces this content area.
                    The browser history is also updated, so back/forward buttons work correctly.
                </p>

                <div class="note">
                    <strong>Zero JavaScript!</strong> Active nav state is handled declaratively with
                    <code>d-signal</code> and <code>d-class</code>.
                </div>
            </div>
        </main>
    </div>

    <%- include('../../partials/core-scripts') %>
</body>

</html>