<aside class="dashboard-sidebar">
    <a href="/" d-pulse="navigate" d-target="html" class="sidebar-logo">
        <img src="/brand/surf-logo.png" alt="SURF Logo" class="logo-img">
        <div class="logo-text-group">
            <span class="logo-brand">Surf</span>
            <div class="logo-badges">
                <span class="badge-beta">Beta</span>
                <span class="version-tag">v0.2.0</span>
            </div>
        </div>
    </a>

    <div class="sidebar-group">
        <h3 class="group-title">Workspace</h3>
        <nav class="nav-menu">
            <a href="/examples/kanban" class="nav-item" d-pulse="navigate" d-target="html">
                <span class="icon">⊞</span>
                Board
            </a>
            <a href="/examples/kanban/backlog" class="nav-item" d-pulse="navigate" d-target="html">
                <span class="icon">≣</span>
                Planning
            </a>
        </nav>
    </div>

    <div class="sidebar-footer">
        <div class="engine-card">
            <div class="engine-header">
                <span class="flash-icon">⚡</span>
                <span class="engine-title">SURF Engine</span>
            </div>
            <p class="engine-status">Running <span class="version">v0.2.0</span>. Server-driven reactivity is active.
            </p>
        </div>
    </div>
</aside>

<style>
    /* Sidebar Styles */
    .dashboard-sidebar {
        width: 260px;
        background: rgba(15, 23, 42, 0.6);
        border-right: 1px solid var(--border-subtle);
        backdrop-filter: blur(12px);
        display: flex;
        flex-direction: column;
        padding: 1.5rem;
        gap: 2rem;
        height: 100vh;
        /* Fixed height for sticky sidebar */
    }

    .sidebar-logo {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding-bottom: 2rem;
        text-decoration: none;
        transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .sidebar-logo:hover {
        transform: translateY(-2px);
        opacity: 0.9;
    }

    .logo-img {
        height: 36px;
        width: 36px;
        object-fit: contain;
        filter: drop-shadow(0 0 8px rgba(56, 189, 248, 0.3));
    }

    .logo-text-group {
        display: flex;
        flex-direction: column;
        line-height: 1.1;
    }

    .logo-brand {
        font-weight: 800;
        font-size: 1.4rem;
        letter-spacing: -0.02em;
        background: linear-gradient(135deg, #fff, var(--brand-primary), var(--cyan));
        background-clip: text;
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .logo-badges {
        display: flex;
        align-items: center;
        gap: 0.4rem;
        margin-top: 0.2rem;
    }

    .badge-beta {
        font-size: 0.55rem;
        background: var(--brand-secondary);
        color: #0f172a;
        padding: 1px 4px;
        border-radius: 4px;
        text-transform: uppercase;
        font-weight: 800;
        letter-spacing: 0.05em;
    }

    .version-tag {
        font-size: 0.6rem;
        color: var(--text-muted);
        font-family: monospace;
        opacity: 0.6;
    }

    .group-title {
        font-size: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        color: var(--text-muted);
        margin-bottom: 0.75rem;
        font-weight: 600;
        padding-left: 0.75rem;
    }

    .nav-menu {
        display: flex;
        flex-direction: column;
        gap: 0.25rem;
    }

    .nav-item {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding: 0.6rem 0.75rem;
        color: var(--text-muted);
        text-decoration: none;
        border-radius: 6px;
        font-size: 0.9rem;
        font-weight: 500;
        transition: all 0.2s;
    }

    .nav-item:hover {
        background: rgba(255, 255, 255, 0.05);
        color: var(--text-main);
    }

    .nav-item.active {
        background: rgba(56, 189, 248, 0.1);
        color: var(--brand-primary);
    }

    .nav-item .icon {
        font-size: 1.1rem;
        width: 20px;
        text-align: center;
    }

    .dot {
        width: 8px;
        height: 8px;
        border-radius: 50%;
    }

    .dot.success {
        background: var(--success);
        box-shadow: 0 0 8px var(--success);
    }

    .dot.info {
        background: var(--brand-primary);
        box-shadow: 0 0 8px var(--brand-primary);
    }

    .dot.warning {
        background: var(--danger);
        box-shadow: 0 0 8px var(--danger);
    }

    /* Sidebar Footer */
    .sidebar-footer {
        margin-top: auto;
    }

    .engine-card {
        background: rgba(30, 41, 59, 0.5);
        border-radius: 8px;
        padding: 1rem;
        border: 1px solid var(--border-subtle);
    }

    .engine-header {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        margin-bottom: 0.5rem;
    }

    .flash-icon {
        color: var(--brand-primary);
    }

    .engine-title {
        font-weight: 700;
        font-size: 0.85rem;
    }

    .engine-status {
        font-size: 0.75rem;
        color: var(--text-muted);
        line-height: 1.4;
    }

    .version {
        color: var(--brand-secondary);
        font-family: monospace;
    }
</style>