<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Surf Board - Project Management</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap');

        :root {
            --bg-body: #0f172a;
            --bg-sidebar: rgba(15, 23, 42, 0.95);
            --bg-column: rgba(30, 41, 59, 0.4);
            --bg-card: #1e293b;
            --bg-card-hover: #334155;

            --text-main: #f8fafc;
            --text-muted: #94a3b8;

            --border-subtle: rgba(255, 255, 255, 0.08);
            --border-focus: rgba(56, 189, 248, 0.5);

            --brand-primary: #38bdf8;
            /* Sky Blue */
            --brand-secondary: #818cf8;
            /* Indigo */
            --danger: #ef4444;
            --success: #10b981;
            --warning: #f59e0b;

            --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
            --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
            --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Inter', system-ui, -apple-system, sans-serif;
            background: radial-gradient(circle at top right, #1e1b4b, #0f172a);
            background-attachment: fixed;
            color: var(--text-main);
            height: 100vh;
            overflow: hidden;
            line-height: 1.5;
            -webkit-font-smoothing: antialiased;
        }

        /* Dashboard Layout */
        .dashboard-layout {
            display: grid;
            grid-template-columns: 260px 1fr;
            height: 100vh;
            overflow: hidden;
        }

        .dashboard-main {
            display: flex;
            flex-direction: column;
            overflow: hidden;
            position: relative;
        }

        .dashboard-content {
            flex: 1;
            overflow: auto;
            /* Enable both scrollbars */
            padding: 2rem;
            position: relative;
        }

        .board-container {
            min-width: 100%;
            display: flex;
            flex-direction: column;
            gap: 2rem;
        }

        /* Board Specifics */
        .kanban-board {
            display: flex;
            align-items: flex-start;
            gap: 2rem;
            min-height: 500px;
            /* Scrollbar space */
        }

        /* Column Styles */
        .column {
            min-width: 320px;
            width: 320px;
            max-height: 100%;
            display: flex;
            flex-direction: column;
            background: var(--bg-column);
            border: 1px solid var(--border-subtle);
            border-radius: 12px;
            padding: 1rem;
            backdrop-filter: blur(10px);
            transition: border-color 0.2s;
        }

        .column:hover {
            border-color: rgba(255, 255, 255, 0.15);
        }

        .column-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1rem;
            padding: 0 0.25rem;
        }

        .header-title {
            font-weight: 600;
            font-size: 0.9rem;
            color: var(--text-main);
            display: flex;
            align-items: center;
            gap: 0.5rem;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        .dot-indicator {
            width: 8px;
            height: 8px;
            border-radius: 50%;
        }

        .dot-indicator.todo {
            background: var(--text-muted);
        }

        .dot-indicator.doing {
            background: var(--brand-primary);
            box-shadow: 0 0 8px rgba(56, 189, 248, 0.4);
        }

        .dot-indicator.done {
            background: var(--success);
        }

        .column-count {
            background: rgba(255, 255, 255, 0.05);
            padding: 2px 8px;
            border-radius: 12px;
            font-size: 0.75rem;
            color: var(--text-muted);
        }

        .task-list {
            flex: 1;
            overflow-y: auto;
            display: flex;
            flex-direction: column;
            gap: 1rem;
            padding-right: 4px;
            /* Space for scrollbar */
            min-height: 100px;
        }

        /* Scrollbar styling for column */
        .task-list::-webkit-scrollbar {
            width: 4px;
        }

        .task-list::-webkit-scrollbar-track {
            background: transparent;
        }

        .task-list::-webkit-scrollbar-thumb {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 4px;
        }

        .add-task-form {
            margin-top: 1rem;
        }

        .ghost-add-btn {
            width: 100%;
            text-align: left;
            background: transparent;
            border: 1px dashed var(--border-subtle);
            color: var(--text-muted);
            padding: 0.75rem;
            border-radius: 8px;
            cursor: pointer;
            font-size: 0.9rem;
            transition: all 0.2s;
        }

        .ghost-add-btn:hover {
            border-color: var(--text-muted);
            color: var(--text-main);
            background: rgba(255, 255, 255, 0.02);
        }

        .add-input {
            width: 100%;
            padding: 0.75rem;
            background: var(--bg-card);
            border: 1px solid var(--border-focus);
            border-radius: 8px;
            color: var(--text-main);
            outline: none;
            animation: fadeIn 0.1s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(5px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>

<body>
    <div class="dashboard-layout">
        <%- include('partials/sidebar.ejs') %>

            <div class="dashboard-main">
                <%- include('partials/header.ejs') %>

                    <!-- Sprint Selector / Board Header -->
                    <div style="padding: 0 2rem; margin-top: 1rem; display: flex; gap: 1rem; align-items: center;">
                        <form action="/examples/kanban" method="GET" d-pulse="navigate" d-target=".board-container"
                            style="display: flex; gap: 0.5rem; align-items: center;">
                            <label for="sprint-select"
                                style="font-weight: 500; font-size: 0.9rem; color: var(--text-muted);">Active
                                Sprint:</label>
                            <select id="sprint-select" name="sprintId" d-signal="change: submit"
                                style="background: var(--bg-card); border: 1px solid var(--border-subtle); color: var(--text-main); padding: 0.5rem; border-radius: 6px; min-width: 150px;">
                                <option value="" disabled <%=!activeSprintId ? 'selected' : '' %>>Select Sprint</option>
                                <% sprints.forEach(function(sprint) { %>
                                    <option value="<%= sprint.id %>" <%=activeSprintId===sprint.id ? 'selected' : '' %>>
                                        <%= sprint.title %>
                                    </option>
                                    <% }); %>
                            </select>
                        </form>

                        <% if (activeSprintId) { %>
                            <div
                                style="font-size: 0.8rem; color: var(--brand-secondary); background: rgba(129, 140, 248, 0.1); padding: 4px 8px; border-radius: 4px;">
                                <%= sprints.find(s=> s.id === activeSprintId)?.status || 'Active' %>
                            </div>
                            <% } %>
                    </div>

                    <div class="dashboard-content">
                        <div class="board-container">
                            <% if (activeSprintId) { %>
                                <div class="kanban-board" d-auto-refresh="3000"
                                    d-auto-refresh-url="/examples/kanban?sprintId=<%= activeSprintId %>">
                                    <% board.forEach(col=> { %>
                                        <%- include('partials/column.ejs', { column: col, activeSprintId: activeSprintId
                                            }) %>
                                            <% }); %>

                                                <!-- Add Column Placeholder (Visual Only) -->
                                                <div
                                                    style="min-width: 300px; height: 60px; border: 2px dashed var(--border-subtle); border-radius: 12px; display: flex; align-items: center; justify-content: center; color: var(--text-muted); cursor: pointer; opacity: 0.6;">
                                                    + Add Section
                                                </div>
                                </div>
                                <% } else { %>
                                    <div
                                        style="text-align: center; padding: 4rem; color: var(--text-muted); border: 2px dashed var(--border-subtle); border-radius: 12px;">
                                        <h3>Select a Sprint to view the board</h3>
                                        <p style="margin-top: 1rem;">
                                            No active sprint selected.
                                            <a href="/examples/kanban/backlog"
                                                style="color: var(--brand-primary); text-decoration: none; border-bottom: 1px dashed var(--brand-primary);">
                                                Go to Backlog
                                            </a>
                                            to create a new sprint or add tasks.
                                        </p>
                                    </div>
                                    <% } %>


                        </div>
                    </div>
            </div>
    </div>

    <!-- Global Task Detail Modal -->
    <dialog id="task-detail-modal"
        style="border:none; background:transparent; padding:0; outline:none; backdrop-filter:blur(4px);">
        <!-- Content will be injected here via Surf -->
    </dialog>

    <%- include('partials/scripts.ejs') %>
        <style>
            .task-list {
                transition: background-color 0.2s;
                min-height: 100px;
                /* Ensure drop zone exists even if empty */
            }

            .task-list.drag-over {
                background-color: rgba(56, 189, 248, 0.1);
                /* Light blue tint */
                border-radius: 8px;
                outline: 2px dashed var(--brand-primary);
            }
        </style>
</body>

</html>